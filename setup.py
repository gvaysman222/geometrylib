from setuptools import setup, find_packages

setup(
    name="geometrylib",
    version="0.1.0",
    description="Библиотека для расчёта площади фигур",
    author="Ваше Имя",
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
